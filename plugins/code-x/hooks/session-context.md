<EXTREMELY_IMPORTANT>
Code-X is installed — a plan-then-build methodology for a non-coder director. Do not skip the process, and do not work from memory of it: the rules live in files — read them.

IRON LAWS (always in force):
1. NEVER build before the WRITING stage passes the checker.
2. NEVER end a session without a handoff and a paste-ready resume prompt.

AT SESSION START: find this project's Code-X handoff; if one exists, read it and resume from its prompt before doing anything else.

FOR ANY real build / design / fix / extend work (non-negotiable): first read the protocol files in this plugin's skill folder, in order — START-HERE.md, then KERNEL.md, GATES.md, ROUTING.md, SEVERITY.md (packet floor: PACKET-CONTENTS.md + BUILDER-STANDARD.md). Full router: SKILL.md. Checker: run python3 checkers/cx --help and python3 checkers/tests/run.py. Never build before cx check passes.

Quick one-off questions don't need the full machinery — but the two iron laws still hold.
</EXTREMELY_IMPORTANT>
