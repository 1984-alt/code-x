# BAD — missing detection_command (cannot prove the whole class is found).
class_sweep:
  class: "unredacted account-number in log lines"
  scope_boundary: "app/parsers/*.py"
  pre_fix_hits: ["app/parsers/parser_a.py:212"]
  post_fix_count: 0
  positive_control:
    planted_instance_detected: yes
  regression_test_ref: tests/test_x.py
