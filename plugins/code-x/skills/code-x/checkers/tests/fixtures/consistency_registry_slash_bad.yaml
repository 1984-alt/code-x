# Fixture: registry pointing to a rule-bearing file with slash-shorthand PROP ids
# Used by: CONSISTENCY-SLASH-SHORTHAND-PROP contract test
# Expected: FIX-FIRST — B-PROP-001/006 detected in the scoped canon file
scan_scope:
  rule_bearing:
    - checkers/tests/fixtures/consistency_slash_bad_canon.md

rules:
  - id: test-rule-slash
    canonical: "one-shot review pattern"
    key_phrases: ["one-shot review", "ENFORCES"]
    appears_in:
      - checkers/tests/fixtures/consistency_slash_bad_canon.md
