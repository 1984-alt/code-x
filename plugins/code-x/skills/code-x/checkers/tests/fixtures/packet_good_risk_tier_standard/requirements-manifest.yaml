# requirements-manifest.yaml — inside the frozen packet hash (PROP-007).
# Dispositions: BUILDING | NOT_BUILDING | NOT_APPLICABLE | CEO_DEFERRED.
# NOT_BUILDING / CEO_DEFERRED rows need a ceo_decision_ref resolving to a ledger row.
# PBF-PROP-019 Phase 5 (EVAL-052): STANDARD declared with a resolving risk_tier_decision_ref
# (good fixture) — the positive-control tier for the high-risk-force spine test (proves the
# CARD-HIGH-RISK-FORCES-FOUNDATION gate ignores the tier across ALL three values, not just LITE).
risk_tier: STANDARD
risk_tier_decision_ref: CEO-D-001
requirements:
  - id: REQ-001
    disposition: BUILDING
    acceptance_criterion:
      pass_condition: "Submitting valid credentials on the login screen lands on the dashboard and creates a session cookie."
      evidence_type: automated_test
      verification_ref: "tests/test_login.py::test_valid_login_lands_dashboard"
