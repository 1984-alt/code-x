# Security baseline — cx fixture project
Authn/authz: N/A runtime, but documented here as required.
Secrets: none in fixtures; referenced by name only.
Input validation: YAML parsed via safe_load only.
Retention/audit: fixtures live in git history.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
