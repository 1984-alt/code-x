# GOOD (PB-PROP-003 Unit 2, §R5 migration proof): pb_prop_003_legacy_packet never declared the
# pb_prop_003_wiring marker — the OLD free-text criteria_ref is accepted, recorded as a non-blocking
# (P2) legacy_criteria_ref advisory. rc must still be 0 (advisories never block).
module_acceptance:
  module_id: m_legacy
  verify_app:
    passed: true
    repo_sha: 7d1408c9aa21
    generated_by: verify-app-agent
    criteria_ref: cards/m_legacy-home-card.yaml#acceptance_criteria
