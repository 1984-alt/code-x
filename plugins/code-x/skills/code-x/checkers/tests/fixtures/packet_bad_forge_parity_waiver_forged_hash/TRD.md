# TRD — cx fixture project
Architecture: flat files checked by cx check packet.
No services, no runtime — the checker is the consumer.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
fixture filler — non-trivial content floor.
