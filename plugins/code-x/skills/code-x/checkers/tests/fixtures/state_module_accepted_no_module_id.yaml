# State referencing a sha-bound but module-LESS acceptance receipt for m1 (GPT R10 P0 bite).
# acceptance_sha12 = sha12(module_acceptance_no_module_id.yaml) — the file↔state binding is intact;
# the defect is that the receipt does not identify its module.
project: cx-module-acceptance-test
protocol_stamp: Code-X V1
current_module: m2
accepted_modules:
  - module_id: m1
    acceptance_ref: module_acceptance_no_module_id.yaml
    acceptance_sha12: "62fbdb0e9fc9"
