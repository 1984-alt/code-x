# RENDER-FIT-PROFILE-UNPINNED (P0): render_profile present but profile_hash empty.
# Everything else is valid; coverage_matrix is non-UI so ONLY the unpinned-profile clause
# fires (an empty profile_hash leaves the fp/viewport pins unestablished, so the per-row
# profile/viewport checks stay silent — exactly the unpinned hole). Run with: --repo-head aaaaaaaaaaaa.
current_repo_head: aaaaaaaaaaaa

render_profile:
  chromium_revision: "1234.5"
  viewport: "390x844"
  viewports:
    - viewport_id: phone
      width: 390
  profile_hash: ""

coverage_matrix:
  ui_card: false
  required_rows: []

render_evidence:
  - card_id: BUILD-RF-001
    screen_id: home
    viewport_id: phone
    theme: light
    content_state: populated
    route: /home
    repo_head: aaaaaaaaaaaa
    state_sha12: state00aabbcc
    locked_packet_hash: pkt00112233aa
    render_profile_hash: profilehash01
    tool_version: "cx-render/1.0.0"
    command: "cx render collect --screen home"
    generated_by: cx render collect
    screenshot_path: render_shot.txt
    screenshot_hash: 3e4f45cb0a7d
    measured_metrics:
      viewport_width: 390
      content_width: 390
      has_horizontal_overflow: false
      max_visible_right: 389.4
      nonblank: true
      app_ready: true
      controls_in_frame:
        - id: add_expense
          in_frame: true
    produced_at: "2026-06-22T09:00:00Z"
