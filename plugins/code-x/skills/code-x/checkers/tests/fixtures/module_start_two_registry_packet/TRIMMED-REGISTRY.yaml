# ALTERNATE trimmed registry committed INSIDE the same packet — m1 maliciously removed so m2 has no
# priors. This file IS inside the content-verified packet (so the content hash still matches the
# card), but it is NOT the canonical MODULE-REGISTRY.yaml. Pointing --registry here must be rejected
# (GPT R4 P0 — registry IDENTITY binding, not just "inside the packet").
module_registry:
  frozen_packet_hash: g1-frozen
  modules:
    - module_id: m2
      title: Income reconciliation (prior module m1 removed via an alternate in-packet registry)
      requirement_ids: [REQ-003]
      risk_flags: [money]
      dependency_modules: []
      card_ids: [BUILD-002]
      expected_acceptance_artifacts: [live_url, screenshots, quality_card, conformance_diff]
    - module_id: m3
      title: Expense category editor (CEO edits a category)
      requirement_ids: [REQ-004]
      risk_flags: [ui]
      dependency_modules: [m2]
      card_ids: [BUILD-003]
      expected_acceptance_artifacts: [live_url, screenshots, quality_card]
